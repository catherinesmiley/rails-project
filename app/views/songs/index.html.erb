<h2>All Songs</h2>

<%= form_tag(songs_path, method: "get") do %>
    <%= label_tag :search %>
    <%= text_field_tag (:search) %>
<%= submit_tag ("Search") %>
<% end %>

<% @songs.each do |song| %>
    <p><%= link_to song.title, song_path(song) %> by: <%= song.artist %><p>
<% end %>